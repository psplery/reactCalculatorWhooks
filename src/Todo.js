import React from "react";

const Todo = () => {
  return (
    <div className="App">
      <h2>Simple listing of things to be added/changed</h2>
      <p>Add decimal calculation</p>
      <p>Add Clear Button ("C" - Clear All, i.e. Reset)</p>
      <p>Add Clear Entry Button ("CE" - Clear last digit of current entry)</p>
      <p>
        Evaluate input on clicking on any operation button (as of now: only
        enter results in evaluation)
      </p>
      <p> ... </p>
    </div>
  );
};

export default Todo;
